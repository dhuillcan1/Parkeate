</br>
 <div class="container theme-showcase" role="main">
<!-- Main jumbotron for a primary marketing message or call to action -->
        <div class="jumbotron jumbotron-fluid">
<%= form_with(model: usertype, local: true) do |form| %>
  <% if usertype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usertype.errors.count, "error") %> prohibited this usertype from being saved:</h2>

      <ul>
      <% usertype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="container">
             <h1 class="display-4">Descrición de usuarios</h1>
            <div class="row">
              <div class="col-md-6 col-md-offset-3">

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :usertype_descripcion, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit "Registrarse", class: "btn btn-info"  %>
  </div>
<% end %>
