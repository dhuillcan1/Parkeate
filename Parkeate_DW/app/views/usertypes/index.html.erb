<br>
<p id="notice"><%= notice %></p>


<div class="jumbotron jumbotron-fluid">

  <div class="container">
   <h1 class="display-4">Listado de usuarios</h1>
                <div class="row">
            <div class="col">
          <table class="table table-striped">
        <thead>
        </thead>

        <tbody>
          <% @usertypes.each do |usertype| %>
            <tr>
              <td><%= usertype.descripcion %></td>
              <td><%= link_to 'Mostrar', usertype, class: "btn btn-warning"   %></td>
              <td><%= link_to 'Editar', edit_usertype_path(usertype), class: "btn btn-warning"   %></td>
              <td><%= link_to 'Eliminar', usertype,, class: "btn btn-danger"   method: :delete, data: { confirm: 'Estas segura' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

<br>

<%= link_to 'Nuevo tipo de usuario', new_usertype_path, class: "btn btn-info"   %>
